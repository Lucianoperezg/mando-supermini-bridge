// ============================================================
// secrets.h — credenciales locales (NO commitear)
//
// Copia este fichero como secrets.h y rellena tus valores:
//   cp include/secrets.h.example include/secrets.h
//
// El fichero secrets.h está en .gitignore y nunca se sube
// al repositorio.
// ============================================================

#pragma once

// Código de emparejamiento HomeKit (8 dígitos, sin guiones).
// Elige uno que no sea trivial (p. ej. evita "12345678").
#define HOMEKIT_PAIRING_CODE  "00000000"  // ← cambia por tu código real (8 dígitos numéricos)
